# Athlete data

This directory is the coach's memory about one athlete: context/, plan/, log/, data/, knowledge/.
The coach is run here by the harness (the `coach` repo), which supplies its instructions. Nothing in this
directory is code. It is a private git repository; the coach commits its own changes.
